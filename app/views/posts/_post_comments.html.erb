<small class="text-muted">View all<%= pluralize(post.comments.count, "comments") %></small>
<% posts.comments.each do %>
    <p class="card-text m-0"><span class="fw-bold"><%= comment.user.username%></span><%= comment.body %></p>
<%end%>